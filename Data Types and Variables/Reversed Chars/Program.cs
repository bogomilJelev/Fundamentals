using System;
using System.IO;

namespace da_da
{
    class Program
    {
        static void Main(string[] args)
        {
            char first = char.Parse(Console.ReadLine());
            char first1 = char.Parse(Console.ReadLine());
            char first2 = char.Parse(Console.ReadLine());
            Console.WriteLine($"{first2} {first1} {first}");
        }
    }
}
